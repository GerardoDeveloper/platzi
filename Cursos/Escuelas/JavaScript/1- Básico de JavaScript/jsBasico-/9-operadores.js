
/**
 * =============================Binary operators======================== *
 *
 * Esto es porque significa que están 2 operandos involucrado.
 */

// de operación

3 + 2 // = 5
50 - 10 // = 40
10 * 3 // = 30
20 / 2 // =10

'Diego ' + 'De Granda'

/**
 * =============================Unary operator======================== *
 *
 * Aquí es porque hay solo 1 operando involucrado.
 */

/**
 * =============================Lógicos============================ *
 */

!false // operador not (no)

/**
 * =============================Asignación===========================*
 */

var a = 1;

/**
 * =============================comparación========================= *
 */

3 == '3' // es igual

3 === '3' // es estrictamente igual

5 < 3, // menor

5 <= 3, // menor o igual

5 > 3, //mayo

5 >= 3 // mayor o igual

a && b // operador 'and' (y), este operador genera una valicación siempe y cuando AMBAS variables sean verdad, "var 1 y var 2"

true || false // operador 'or' (o), este operador genera una validacion siempre y cuando CUALQUIER de las variables sea veradera, "var 1 o var 2"


/* ==================================Operador que solo se pueden utilizar con variables============================== */

var edad = 40;

edad++;  // operador de incremento por 1
edad += 2;

edad;