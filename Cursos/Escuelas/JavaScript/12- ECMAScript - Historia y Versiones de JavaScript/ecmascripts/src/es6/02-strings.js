// antes de ES6
let hello = 'Hello';
let word = 'word';
let epicPhase = hello + ' ' + word;
console.log(epicPhase);

// En ES6
let epicPhase2 = `${hello} ${word}`;
console.log(epicPhase);

// Multiline string
let lorem = 'esto es un string \n' + 'esto es otra linea';

// ES6
let lorem2 = `Esta es una linea
Esta es otra linea.
`;